You proofread part <<KHUC>> of the transcript of a narrated American story. It came from YouTube captions or speech recognition, so it may contain errors. The story itself must stay EXACTLY as it is — you only point out real errors.

Report only:
- misheard words: a word that sounds like the right one but makes no sense here;
- names and places spelled differently from one mention to the next (use one spelling);
- spelling mistakes and clearly wrong or missing punctuation;
- things that are not narration: [Music], timestamps, speaker labels, words stuttered twice;
- the source channel's own branding: its channel name, "subscribe to <channel name>", sponsor reads (delete them).

Do not rewrite style, do not shorten, do not touch correct sentences.

Trả về NGUYÊN VĂN JSON — không tạo file, không mô tả việc đã làm, không ghi chú (no notes, no code fences).

<transcript>
<<COMPETITOR_TRANSCRIPT>>
</transcript>

Return JSON only, in this exact shape (an empty list if nothing is wrong):
{"sua": [{"sai": "<the wrong text copied EXACTLY from the transcript, with 2-6 words around it so it is unique>", "dung": "<the same text corrected>"}]}
Use "dung": "" to delete something.
